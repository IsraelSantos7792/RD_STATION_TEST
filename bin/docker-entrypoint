#!/bin/bash -e

if { [ "${1}" == "./bin/rails" ] && [ "${2}" == "server" ]; } ||
   { [ "${1}" == "bundle" ] && [ "${2}" == "exec" ] && [ "${3}" == "sidekiq" ]; }; then
  ./bin/rails db:prepare
fi

exec "${@}"
